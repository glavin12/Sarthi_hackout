services:
  data-service:
    build: ./data-service
    ports:
      - "8000:8000"
    environment:
      DATA_DB_PATH: /app/data.db
    # No volume on purpose: recreating the container reseeds (the demo reset path).

  # --- teammates: uncomment and adjust your service (names/ports are guesses) ---
  # understanding-service:
  #   build: ./understanding-service
  #   ports: ["8001:8001"]
  # recommendation-service:
  #   build: ./recommendation-service
  #   ports: ["8002:8002"]
  # chatbot-service:
  #   build: ./chatbot-service
  #   ports: ["8003:8003"]
  # frontend:
  #   build: ./frontend
  #   ports: ["3000:3000"]
